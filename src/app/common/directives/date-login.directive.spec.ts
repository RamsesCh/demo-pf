import { DateLoginDirective } from './date-login.directive';

describe('DateLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new DateLoginDirective();
    expect(directive).toBeTruthy();
  });
});
